# Path to the OIE engine source tree (for compile-time dependencies)
engine.dir=../engine

# Plugin version
plugin.version=1.0.1

# OIE version compatibility
oie.version=4.5.2

# Build directories
src.dir=src
build.dir=build
classes.dir=${build.dir}/classes
dist.dir=dist
lib.dir=lib
